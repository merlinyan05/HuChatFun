# CLAUDE.md — HuChatFun 项目记忆

> Claude Code 每次启动自动读取此文件。保持更新。
> **规则：每完成一个阶段，立即更新"当前进度"和"上次会话摘要"，不等用户提醒。**

## 项目是什么

HuChatFun：基于户晨风（B站直播博主）语料微调的 AI 对话模型。纯娱乐项目，非官方。
目标是让模型说话比真人的口头禅更密集、观点更爆——做一个"浓缩版户晨风"。

## 技术路线

- 基座：Qwen3.5-9B-Instruct（Qwen3.5 出了再换）
- 微调：QLoRA（4-bit NF4 + LoRA r=64）
- 训练机：RTX 5080 16GB
- 推理：Mac Mini M4 16GB → Ollama → OpenClaw
- 语料：`corpus/HuChenFeng-1.1/` 下约 400+ 场直播文字稿（.md 格式）（但是可能只挑近几年的）

## 目录结构

```
corpus/          原始语料（只读）
pipeline/        数据处理脚本（step0-step6，按编号顺序跑）
data/            各阶段数据产物（step1_cleaned → ... → final/）
training/        训练脚本（train.py, merge_lora.py, convert_gguf.sh）
eval/            评估脚本和测试数据
deploy/          Ollama Modelfile + OpenClaw 配置
models/          模型文件（gitignore，不入库）
logs/            实验记录
docs/            项目文档
```

## 当前进度

<!-- 每次会话结束前更新这里 -->

- [X] 项目规划完成
- [X] 清洗策略设计完成
- [X] 工作流文档完成
- [X] 阶段0：语料格式探查（完成于 2026-03-31）
- [X] 阶段1：粗切去噪（完成于 2026-03-31）
- [X] 阶段2：结构化切分（完成于 2026-03-31）
- [X] 阶段3：质量评分过滤（完成于 2026-03-31）
- [X] 阶段4：构造训练对（完成于 2026-03-31）
- [ ] 阶段5：风格增强（可选，暂跳过）
- [ ] 阶段6：输出 train.json / eval.json
- [ ] 首次训练
- [ ] 评估 & 迭代
- [ ] 部署到 Ollama

## 关键设计决策（已确定）

1. **不喂完整直播**，只喂精华段落。原始语料 60% 是感谢礼物/等人的废话。
2. **三管齐下提高口头禅密度**：数据只保留金句 + system prompt 显式要求 + LLM 改写增强。
3. **数据管线分 6 步**，每步独立脚本，中间产物隔离，可单独重跑。
4. **训练数据格式**：Qwen ChatML（`<|im_start|>system/user/assistant<|im_end|>`）。
5. **先不做阶段5**（风格增强），等第一版模型效果出来再决定。

## 语料格式备忘

<!-- 已由 step0_explore.py 探查确认（2026-03-31）-->

- 说话人标记格式：`户晨风：xxx` / `某网友：xxx`（高度统一）
- 有无时间戳：**无**，全部 494 个文件均无时间戳
- INC 后缀文件：共 28 个，从 `某网友：` 开头，是连麦中途才开始录制的片段
- 不同时期格式：**基本一致**，2023～2025 格式相同；极少数（5 个）含 Markdown
- 总文件数：494（466 正常 + 28 INC），其中有效对话文件约 490 个
- 总发言轮数：户晨风 202,427 轮 / 某网友 202,268 轮
- 单场最长：2023-04-18.md（1062 轮，72,818 字符）

### 需要 step1 处理的异常文件（9 个）

| 文件 | 问题 | 处理方式 |
|------|------|----------|
| `Preface.md`、`Acknowledgements.md`、`SUMMARY.md`、`videos.md` | 元数据，不是对话 | 直接跳过 |
| `2025-02-27.md`、`2025-03-04.md` | 含 `【API调用失败` 说话人 | 删除该行 |
| `2024-07-21.md` 等其余 3 个 | 说话人标记里混入了一段话 | 截断到第一个 `：` |

## 需要注意的坑

- 正则去噪不能太猛，户晨风正常对话里也会说"感谢"，别误删观点段落
- .md 文件里极少有 markdown 格式（494 个里只有 5 个），不是主要问题，但仍需 strip
- 2023 年和 2025 年的说话风格可能不同（"安卓"梗是后来才密集的）
- 训练数据的 user/assistant 必须严格交替，不能有连续同角色
- step1 跳过非对话文件：Preface.md / Acknowledgements.md / SUMMARY.md / videos.md

## 户晨风核心口头禅（训练目标）

- "安卓" = 万能贬义词（安卓人/安卓逻辑/安卓学历/安卓网/安卓水）
- "纯纯" = 强调词（纯纯低论/纯纯废了/纯纯安卓）
- "购买力" = 核心衡量指标
- "前程似锦" = 反讽，意思是没希望
- "我告诉你" / "你给我记住了" = 引出观点
- "知道吧" / "能明白吗" = 收尾确认
- "咱有什么说什么" = 表示实话实说
- "你废了" = 判定对方没救

## 上次会话摘要

<!-- 每次结束时让 Claude Code 更新，不等用户提醒 -->

最近一次会话（2026-03-31）：
- 构建了完整目录结构（training/ eval/ deploy/ models/ logs/ docs/ data/step*/final/）
- 添加了 .gitignore（排除 models/、data/各阶段产物、训练产物）
- 完成阶段0：写并执行了 `pipeline/step0_explore.py`，探查了全部 494 个语料文件

产出文件：`pipeline/step0_explore.py`、`.gitignore`

下一步：执行阶段6（输出 train/eval），写 `pipeline/step6_export.py`，把训练对按 9:1 切分成 train.json / eval.json，输出到 `data/final/`。

### 阶段4 结果备忘
- 格式：Qwen ChatML 多轮（system + user/assistant 交替）
- 输入 1,757 segment → 全部转换成功（0 跳过）
- 输出：`data/step4_pairs/train_pairs.jsonl`（1,757 条）
- 总对话轮次：46,292，平均每条 26.3 轮
- system prompt 已写入，含核心口头禅和风格描述

### 阶段3 结果备忘
- 评分维度：口头禅密度（40）+ 平均发言长度（30）+ 互动质量（20）- 噪声惩罚（10）
- 阈值定为 40（原 50 太严，只过 8.3%；40 过 60.3% = 1,757 个 segment）
- 输出：`data/step3_scored/scored.jsonl`（全量）+ `passed.jsonl`（1,757 个过线）
- 最高分 62.7，平均分 41.0

### 阶段2 结果备忘
- 切分依据：连麦边界（户晨风说"下一个"/"再见"/"生活愉快"/"前程似锦"等送客信号）
- 总 segment：4,228 → 保留 2,916（69.0%），过滤条件：户晨风 < 3 轮 或 无某网友发言
- 超长段截断：> 60 行截断（避免多话题混入）
- 输出：`data/step2_segmented/`（174 个 .jsonl 文件）
- 格式：每行 `{"source": "2025-xx-xx", "seg_id": N, "lines": [...]}`

### 阶段1 结果备忘
- 只用 2025 年数据，排除 INC，激进去噪（户晨风 < 15 字删除 + 礼物感谢行删除）
- 输入 164,338 行 → 输出 134,964 行（保留 82.1%）
- 2025-03-04.md 清洗后为空（全是 API 转录错误），已跳过
- 输出位置：`data/step1_cleaned/`（174 个 .txt 文件）
